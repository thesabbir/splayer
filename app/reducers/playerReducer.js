/**
 * @flow
 */

/**
 *
 * @param state
 * @param action
 * @returns {Object}
 */
export default function (state: Object = {}, action: Object): Object {
  switch (action.type) {
    default:
      return state;
  }
}
